package app.controllers;

import app.interfaces.ControllerInterface;

// My implementation of an MVC controller does not do much on its own, it manipulates other classes like views and models.
// the controller can perform tasks on its own but only in the background or through the console.

// I have desperately tried to implement a property change listener or observable, but it just didn't work the way I wanted it to, so I abandoned that idea.
public class Controller implements ControllerInterface {

}
